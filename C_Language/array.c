#include<stdio.h>
#include<string.h>
int main()
{
	
//	gets and scanf are for iput
	int number[50];
		printf("Enter your name: ");
//			scanf("%s",&name);
	gets(name);
			printf("Welcome  %s to our website",name);
}